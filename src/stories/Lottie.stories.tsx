import React from 'react';
import { Lottie } from '../components/atoms';

export default {
  title: 'Lottie',
  component: Lottie,
};

const Template: any = () => {
  return <Lottie />;
};

export const Default = Template.bind({});
Default.args = {};
