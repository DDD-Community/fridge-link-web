version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/mara-deploy
    overwrite: yes
permissions:
  - object: /home/ubuntu/mara-deploy
    owner: ubuntu
    group: ubuntu
    mode: 755
